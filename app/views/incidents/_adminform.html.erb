<%= semantic_form_for(@incident) do |f| %>
	<%= f.input :estado, :as => :select, :collection => ['Tecnicos', 'Fechado']%>
	<%= f.input :solucao%>
	<%= f.input :prioridade, :as => :select, :collection => Incident::PRIORITYS%>

<br>
<div class="group navform wat-cf">
  <button class="button" type="submit">
    <%= image_tag("web-app-theme/icons/tick.png", :alt => "#{t("web-app-theme.save", :default => "Save")}") %> <%= t("web-app-theme.save", :default => "Save") %>
  </button>
  <span class="text_button_padding"><%= t("web-app-theme.or", :default => "or") %></span>
	<%= link_to t("web-app-theme.cancel", :default => "Cancel"), incidents_path + '/admin', :class => "text_button_padding link_button" %>
</div>

<% end %>